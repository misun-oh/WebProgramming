<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans 
         https://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context 
         https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- xml 설정파일을 이용하여 IOC컨테이너에 Bean 등록 -->
    
	<!-- 1. DI없이 객체를 생성 -->
	<bean id="sender" class="com.maven.ex.spring.send.EmailSender"/>
	
	<!-- 2. 생성자 주입 
	bean : IOC컨터이너에 객체를 생성
		id : 스프링 컨테이너가 빈(Bean)을 구분하기 위해 부여하는 고유 이름
		class : 패키지명을 포함한 클래스명 
	-->
	<bean id="notificationService" class="com.maven.ex.spring.service.NotificationService">
		<!-- 생성자 주입 
			ref : 다른 Bean객체를 주입
			value : 단순 값(문자, 숫자, 문자열 등 리터럴)을 주입
			<constructor-arg ref="sender"/>
			<constructor-arg value="오미자"/>
		-->
	    <constructor-arg ref="sender"/>
	</bean>

   	<!-- 
   		컴포넌트 스캔  
   		지정된 패키지 및 그 하위 패키지에서 특정 어노테이션이 붙은 클래스를 
   		자동으로 스프링 빈으로 등록하는 기능
	-->
   	<!--
    <context:component-scan base-package="com.example" />
	-->    

</beans>